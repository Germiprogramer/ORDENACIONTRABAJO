class Completarlasespecificaciones:
  def __init__(self, tabla):
    self.tabla = list(tabla)
  def elegirsegmento():
    max = input(int("elige el límite menor: "))
    min = input(int("elige el límite mayor"))
  def sacarmaximo(self, tabla, )